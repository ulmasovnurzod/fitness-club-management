server:
  port: 8080

spring:
  application:
    name: fitness-club

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=fitness_club
    username: postgres
    password: 1996
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.hibernate.SQL: debug

app:
  jwt:
    secret: mySecretKeyForCarRentalSystemThatIsVeryLongAndSecure123456789012345678901234567890
    expiration: 900000
    refresh-expiration: 604800000